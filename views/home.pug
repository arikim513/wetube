extends layouts/main
include mixins/videoBlock

block content
    .videos
        each video in videoItems
            +videoBlock({
                videoFile: video.videoFile,
                title: video.title,
                views: video.views
            })